name: Deploy React App to HostGator

on:
  push:
    branches:
      - main 

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 

      - name: 📦 Install dependencies
        # FIX: Added --force to resolve React dependency conflicts during install
        run: npm install --force

      - name: 🏗 Build Project
        run: npm run build 
        
      - name: 📂 Deploy to HostGator via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          
          # CRITICAL FIX 1: Switched to SFTP (SSH-based) on port 22, required by HostGator
          protocol: sftp
          port: 22
          
          # Local folder created by 'npm run build'
          local-dir: ./out/ 
          
          # CRITICAL FIX 2: Clean absolute path (no brackets or Markdown syntax)
          server-dir: /home3/irineashu/jesusrevivalfans.com/
          
          state-name: .ftp-deploy-sync-state.json